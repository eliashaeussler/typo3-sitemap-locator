filesToModify:
  - path: Documentation/guides.xml
    patterns:
      - 'release="{%version%}"'
  - path: ext_emconf.php
    patterns:
      - "'version' => '{%version%}'"
    reportUnmatched: true

releaseOptions:
  commitMessage: '[RELEASE] Release of EXT:sitemap_locator {%version%}'
  signTag: true
